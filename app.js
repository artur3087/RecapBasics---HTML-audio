const audio = document.getElementById("mp3");  
  audio.volume = 0.1;  // Property .volume describes volume from 0.0 (muted), 0.5 (half volume) to 1.0 (100% volume). It can be added to any variable used for audio...  

// Other example:

// const mp3 = document.getElementById("mp3");
//     mp3.volume = 0.1; 